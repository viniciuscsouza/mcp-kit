{
  "name": "@viniciuscsouza/mcp-kit",
  "version": "0.0.3",
  "description": "A decorator-based framework for building Model Context Protocol (MCP) servers in TypeScript.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "test": "vitest run"
  },
  "dependencies": {
    "reflect-metadata": "^0.1.13"
  },
  "devDependencies": {
    "vitest": "^3.2.4"
  }
}
